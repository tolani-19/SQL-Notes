#The where statement or clause allows us to set a condition upon which we will specify which part of the data we want to retrieve from the database.

SELECT 
    *
FROM
    employees
WHERE
    first_name = 'Denis';
    
#Assignment
SELECT 
    *
FROM
    employees
WHERE
    first_name = 'Elvis';